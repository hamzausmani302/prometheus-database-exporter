apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "database-prometheus-exporter.fullname" . }}-config
  labels:
    {{- include "database-prometheus-exporter.labels" . | nindent 4 }}
data:
  config.yaml: |
{{ .Values.config | indent 4 }}
