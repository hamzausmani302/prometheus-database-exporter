apiVersion: v1
kind: Service
metadata:
  name: {{ include "database-prometheus-exporter.fullname" . }}
  labels:
    {{- include "database-prometheus-exporter.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: metrics
      port: {{ .Values.service.port }}
      targetPort: metrics
      protocol: TCP
  selector:
    {{- include "database-prometheus-exporter.selectorLabels" . | nindent 4 }}
